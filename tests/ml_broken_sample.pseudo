// Sample broken pseudocode file containing common human typos
functon factorial(n)
    if (n <= 1) thenn
        retun 1
    else
        retun n * factorial(n - 1)
    endif
endfunction

fr i = 1 to 5 do
    otput factorial(i)
endfor
